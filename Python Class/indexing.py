# indexing operator [] = gives access to a sequence's element (str, list,tuples)

name = "Guga Silva!"

#if(name[0].isupper):
    #name = name.lower()

first_name = name[:5].upper()
last_name= name[5:10].lower()
last_carachter = name[-1]

print(first_name)
print(last_name)
print(last_carachter)